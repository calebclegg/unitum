import Chip from "@mui/material/Chip";

const Tag = () => {
  return (
    <Chip
      label="post"
      variant="outlined"
      color="primary"
      sx={{ height: 20, borderRadius: 0.5 }}
    />
  );
};

export default Tag;
